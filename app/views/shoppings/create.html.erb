<h1>Shopping list for <%= params[:list][:list_id] %></h1>

<table class="table table-hover">
  <tr>
    <th>Item</th>
    <th>Quantity</th>
  </tr>
  
  <% @low_items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.quantity %></td>
    
  </tr>
  <% end %>
</table>